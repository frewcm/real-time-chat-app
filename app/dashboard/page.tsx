import Button from "@/components/Button";
import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <div>
      <Button>hello</Button>
    </div>
  );
};

export default page;
